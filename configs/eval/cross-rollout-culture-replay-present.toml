model = "Qwen/Qwen3.5-4B"
num_tasks = 1
num_rollouts = 10
max_concurrent = 1
push = false
rich = false

[env.taskset]
id = "cross-rollout-culture-v1"
condition = "control"
notes_path = "/tmp/cross-rollout-culture-v1/replay-present-notes.txt"
initial_notes = "Maintenance macro pulse_hold_resume completed. R dispenser is primed."

[env.agent]
harness = { id = "null" }
runtime = { type = "subprocess" }

[sampling]
temperature = 0.7
max_tokens = 1024
