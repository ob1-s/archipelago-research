[project]
name = "archipelago"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bruno Oliveira", email = "brunoadicional2@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "verifiers>=0.3.0",
]

[project.scripts]
archipelago = "archipelago:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
